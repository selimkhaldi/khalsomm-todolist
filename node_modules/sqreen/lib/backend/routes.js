/**
 * Copyright (c) 2016 Sqreen. All Rights Reserved.
 * Please refer to our terms for more information: https://www.sqreen.io/terms.html
 */
'use strict';
const Config = require('../config').getConfig() || { url: require('../config/default').url };

const API_VERSION = 'v0';

module.exports = {
    login: Config.url + '/sqreen/' + API_VERSION + '/app-login',
    logout: Config.url + '/sqreen/' + API_VERSION + '/app-logout',
    metrics: Config.url + '/sqreen/' + API_VERSION + '/metrics',
    beat: Config.url + '/sqreen/' + API_VERSION + '/app-beat',
    exception: Config.url + '/sqreen/' + API_VERSION + '/sqreen_exception',
    commands: Config.url + '/sqreen/' + API_VERSION + '/commands',
    batch: Config.url + '/sqreen/' + API_VERSION + '/batch',
    attack: Config.url + '/sqreen/' + API_VERSION + '/attack',
    rulespack: Config.url + '/sqreen/' + API_VERSION + '/rulespack'
};

